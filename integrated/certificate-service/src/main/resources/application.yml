server:
  port: 8083

spring:
  application:
    name: certificate-service
  datasource:
    url: jdbc:mysql://localhost:3306/certificate_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:tahersahbi7@gmail.com}
    password: ${EMAIL_PASSWORD:ofswwnnicgclvatr}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

springdoc:
  swagger-ui:
    path: /swagger-ui.html

gemini:
  api:
    key: AIzaSyDRjSoMWQKu7htQMlG9TWTjOEU8Nc_EvGA
    url: https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent

certificate:
  verification-base-url: http://localhost:8083/api/certificates/verify
